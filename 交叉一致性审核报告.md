# 交叉一致性审核报告：formula-challenge V2

> 审核范围：domain-model / business-rules / learning-model 三份规格**缺失**，从代码反向提取规格后做跨层交叉核对，并评估 question-model.md 缺失影响。
> 方法：只看不改。证据均为 `文件:行号`。
> 项目根：`formula-challenge-v2/`

---

## 结论

**有条件通过。**

核心链路（Submit → Evaluate → FinalizeReview → LearningState → StudyDay）与 Scheduler 接口在 domain / application / infrastructure 三层签名一致，Attempt 状态机三态与 prisma enum 对齐，BR-030/040/060/070 的唯一约束在 schema 与 domain 双侧一致落地。但存在 **2 个 P1**（End-to-End 题目链路断口、StudyDay 统计字段语义错位/违反评价-评级分离）与若干 P2（死代码守卫函数、BR-071 编号重载、双指针不回写、题型规格缺口），需在进入 Phase 6/7 深化前澄清。

---

## 一、交叉一致性核对总表

| # | 维度 | 结论 | 证据（文件:行） |
|---|------|------|-----------------|
| 1 | 术语一致性："完成"四语义 | ⚠️ 基本区分，但有 2 处名实不符 | 见 §2.1 |
| 1 | 术语："复习" vs review | ✅ 一致 | review-event.ts:2、study-plan.ts:8 |
| 1 | 术语："学习状态" vs LearningState | ✅ 一致（LearningState 为正式类型名） | learning-state.ts:2,8 |
| 1 | 术语："评价" vs Evaluation | ✅ 一致 | evaluation.ts:2,19 |
| 2 | 状态机：AttemptStatus | ⚠️ enum/type 一致，domain 守卫死代码 | 见 §2.2 |
| 2 | 状态机：StudySessionStatus | ⚠️ 同上，守卫死代码 | session.ts:32、end-session.ts:38 |
| 2 | 状态机：SessionItemStatus | ❌ 缺守卫，pending 可直跳 completed | finalize-review.ts:171-172 |
| 2 | 状态机：StudyPlanStatus | ⚠️ draft/expired/completed 从未被任何用例产生 | generate-study-plan.ts:86 |
| 2 | 状态机：ContentItemStatus | ✅（本期无发布流，可接受） | content.ts:25,40 |
| 3 | Scheduler 接口签名 | ✅ 完全一致 | scheduler.ts:11-18 ↔ fsrs-scheduler.ts:44,58 ↔ finalize-review.ts:129,132 |
| 4 | 实体关系：schema vs domain | ❌ 2 处缺口 | 见 §2.4 |
| 5 | 不变量 BR 编号 | ⚠️ BR-071 重载、BR-042 注释引用错名 | 见 §2.5 |
| 6 | question-model 缺失影响 | ❌ 题型/评分规格缺口 | 见 §2.6 |
| 7 | 自相矛盾 | ❌ 3 处 | 见 §2.7 |

---

## 二、逐项结论与证据

### 2.1 术语一致性

**结论：四语义在类型层面已区分，但两处"完成/正确"名实不符。**

- "完成"的四种语义在类型层确实分开命名，未混用：
  - Attempt 完成 = `status: "reviewed"`（attempt.ts:6；finalize-review.ts:167）
  - Review 完成 = 产生 ReviewEvent（review-event.ts:20）
  - PlanItem 完成 = `status: "completed"`（study-plan.ts:9；today-plan.ts:51）
  - SessionItem 完成 = `status: "completed"`（session.ts:20；finalize-review.ts:172）
- ⚠️ **StudyDay.attemptCount 名实不符**：字段名为 attemptCount，却在 `FinalizeReview` 里 +1（finalize-review.ts:183），即统计的是**复习次数**而非作答次数；SubmitAttempt 链路完全不写 StudyDay。
- ❌ **StudyDay.correctCount 用记忆评级冒充正确率**：finalize-review.ts:184 `correctCount + (reviewEvent.rating === "again" ? 0 : 1)`。这把"用户自评非 again"当作"答对"，而真正的对错在 `Evaluation.isCorrect`（evaluation.ts:11）。FinalizeReview 内并未加载 Evaluation，导致答错但用户选 good 的记录也会被计入 correctCount——直接违反 BR-022"评价与记忆评级分离"（evaluation.ts:3 自述）。

### 2.2 状态机一致性

**结论：enum 与 type 取值三层一致；但 domain 守卫函数全部未被调用，SessionItem/StudyPlan 迁移未受控。**

- AttemptStatus：prisma `submitted/evaluated/reviewed`（schema.prisma:37-41）= TS 联合类型（attempt.ts:6）。`canTransitionAttempt`（attempt.ts:24-27）实现严格 +1 步迁移，**但全仓库无任何调用点**（grep 仅命中定义行）。实际检查散落在 application 层：evaluate-attempt.ts:74（必须 submitted）、finalize-review.ts:114（必须 evaluated）。行为等价，但守卫函数是死代码，且 reviewed→reviewed 只能靠 BR-030 幂等分支（finalize-review.ts:97）兜底，而非状态机显式拒绝。
- StudySessionStatus：enum（schema.prisma:58-62）= type（session.ts:7）。`canTransitionSession`（session.ts:32-35）同样**从未被调用**；end-session.ts:38 内联 `status !== "active"`。
- ❌ **SessionItem 迁移失控**：没有 `canTransitionSessionItem`。finalize-review.ts:171-172 只要 `status !== completed/skipped` 就置 completed，允许 **pending → completed 跳过 active**，与 Phase 8 期望的 `pending→active→completed/skipped` 受控路径（审核提示词 Phase 8.5）不一致。
- ⚠️ **StudyPlan 状态机悬空**：`StudyPlanStatus` 含 draft/active/completed/expired（study-plan.ts:7），但 GenerateStudyPlan 一上来就写 `status: "active"`（generate-study-plan.ts:86），没有任何用例产出 draft、也没有 draft→active / →expired / →completed 的迁移。draft 与 expired 是"定义了但永不可达"的状态。
- ContentItemStatus：`isContentVisible` 仅认 published（content.ts:40-42），无迁移函数；因发布流用例尚未实现，本期不构成矛盾。

### 2.3 Scheduler 接口一致性

**结论：完全一致。**

- 接口定义：`initialize(now): LearningStateDraft`、`review(previous, rating, now): LearningStateDraft`（scheduler.ts:11-18）。
- 实现：`FsrsScheduler` 两方法签名逐字对齐（fsrs-scheduler.ts:44, 58-62），rating 经 `RATING_MAP` 映射为 ts-fsrs Grade（fsrs-scheduler.ts:12-21），隔离干净。
- 调用：`scheduler.initialize(reviewedAt)`（finalize-review.ts:129）、`scheduler.review(previous, cmd.rating, reviewedAt)`（finalize-review.ts:132）。
- 方法名、参数个数与类型、返回值均无偏差。✅

### 2.4 实体关系一致性

**结论：schema 与 domain 主关系对齐，但有 2 处明确缺口。**

- ❌ **SessionItem 双指针且不回写（链路断口）**：prisma 中 QuestionInstance.sessionItemId 是真实外键（schema.prisma:387），SessionItem 反向持有 `questionInstances[]`（schema.prisma:427）；同时 SessionItem 又额外挂了一个**无 @relation 的标量** `questionInstanceId String?`（schema.prisma:423）。domain 镜像了这一双指针（session.ts:28、question.ts:22）。但 `GenerateQuestion` 创建实例后**不回写** `sessionItem.questionInstanceId`（generate-question.ts:64-73 只 saveInstance）。而 `SubmitAttempt` 依赖 `sessionItem.questionInstanceId` 查题（submit-attempt.ts:83），为 null 即抛"尚未生成题目实例"。当前端到端只能靠测试**手工**补字段打通（learning-flow.test.ts:335-338），没有任何用例负责把两者绑起来。
- ❌ **Credential 实体无 schema 对应**：domain 定义了 `Credential`（user.ts:18-23）与 `CredentialRepository`（repositories.ts:25-28），register-user.ts:104 也真实写入 credentials；但 prisma schema **没有 Credential 模型**，User 模型（schema.prisma:126-148）也没有 passwordHash 字段。domain 关系在持久层无落点。
- ⚠️ 全仓库无任何 `Prisma*Repository` 实现（infrastructure 仅 fsrs-scheduler.ts、scrypt-password-hasher.ts），schema↔真实持久层关系**尚无法端到端核验**，当前对齐只到类型层。

### 2.5 不变量（BR 编号）一致性

**结论：核心硬不变量双侧一致；但 BR-071 编号重载、BR-042 注释引用了不存在的函数名。**

| BR | schema | domain/注释 | 结论 |
|----|--------|-------------|------|
| BR-030 一 Attempt 一 ReviewEvent | UNIQUE(attemptId)（schema.prisma:479） | review-event.ts:3、finalize-review.ts:97 | ✅ |
| BR-040 UNIQUE(userId,knowledgePointId) | schema.prisma:514 | learning-state.ts:2、finalize-review.ts:146 | ✅ |
| BR-060 UNIQUE(userId,localDate) Plan | schema.prisma:549 | study-plan.ts:14、generate-study-plan.ts:53 | ✅ |
| BR-070 UNIQUE(userId,localDate) StudyDay | schema.prisma:532 | study-day.ts:2 | ✅ |
| BR-012 clientRequestId 幂等 | UNIQUE(userId,clientRequestId)（schema.prisma:457） | attempt.ts:20、submit-attempt.ts:60 | ✅ |
| BR-071 | — | **被两处复用** | ❌ 重载 |
| BR-042 显式初始化 | — | finalize-review.ts:9,122 | ⚠️ 注释错名 |

- ❌ **BR-071 重载**：study-day.ts:2 与 golden-scenario.test.ts:177 把 BR-071 当作 StudyDay 汇总语义；register-user.ts:2 与 identity.test.ts:38 又把 BR-071 当作"注册事务原子性"。同一编号指向两个不同不变量，规格层必须澄清 BR-071 到底管哪件事。
- ⚠️ **BR-042 引用错名 + 死代码**：learning-state.ts:26 注释要求"通过 `createInitialLearningState` 显式初始化"，但实际导出的函数叫 `createInitialLearningStateDraft`（learning-state.ts:39），且**该函数全仓库零调用**——真正的初始化走 `scheduler.initialize()`（finalize-review.ts:129）。注释名与实现名不一致，domain 构造器成了无人调用的死代码。

### 2.6 question-model.md 缺失影响

**结论：QuestionTemplate/QuestionInstance 有定义，但三种题型的生成/评价契约在代码里没有落地，缺失独立 question 规格文档导致覆盖缺口。**

- 存在的部分：`QuestionTemplate` / `QuestionInstance` 已定义（question.ts:10-27），仓库接口齐全（question-repository.ts:7-13），GenerateQuestion 用例可按 knowledgePointType+type 找模板并生成实例、sequence 递增（generate-question.ts:45-74）。✅
- ❌ **题型枚举与 Phase 6 范围不一致**：`QuestionType` 含 4 值 `free_recall/fill_blank/recognition/ordering`（question.ts:8；schema.prisma:30-35），但 Phase 6 验收只要求 3 种；`ordering` 多出且全仓库无任何分支处理（grep 仅命中类型定义行）。
- ❌ **Evaluator 不按题型分支**：唯一实现 `CanonicalMatchEvaluator`（evaluator.ts:33-72）只用 `knowledgePoint.canonicalAnswer` 归一化比对，完全忽略传入的 `questionType`（evaluator.ts:13 收到却不用）。fill_blank 的挖空判定、recognition 的选项/干扰项判定、ordering 的顺序判定均未实现。evaluate-attempt.ts:90 在 template 缺失时还**静默默认 free_recall**，掩盖题型缺失。
- ❌ **QuestionInstance 不存题目 payload**：实例只记录 templateId+sequence（question.ts:20-27），不存渲染后的题干/选项/挖空串；template.config 是无 schema 的 `Json?`（question.ts:16；schema.prisma:371）。无独立 question-model.md 时，"题目长什么样、recognition 的选项从哪来、fill_blank 怎么挖"全部无规格可依。
- ❌ **BR-021"三科目各自实现同契约 Evaluator"仅停留在注释**（evaluator.ts:2-3），代码里只有一个通用 evaluator，没有 Formula/Herb/Acupoint 各自实现。
- 影响：当前仅 free_recall + 精确字符串匹配可端到端跑通；其余题型在"生成→展示→作答→评价"任一环都无契约约束，属规格缺口，不是实现 bug。

### 2.7 自相矛盾

1. ❌ **评价与评级分离被 StudyDay 统计打破**：BR-022 自述"Evaluation 绝不更新 LearningState、评价≠记忆评级"（evaluation.ts:3），但 finalize-review.ts:184 用 `ReviewRating` 反推 `correctCount`，等于让记忆评级污染了"正确率"这一评价侧指标。
2. ❌ **QuestionInstance↔SessionItem 关系运行时不自洽**：关系层要求实例属于 SessionItem（submit-attempt.ts:84 校验 `instance.sessionItemId === sessionItem.id`），但绑定入口 GenerateQuestion 不写回 SessionItem.questionInstanceId（generate-question.ts:64-73），关系靠外部手工维持。
3. ⚠️ **StudyPlan 语义矛盾**：类型定义区分 draft/active/completed/expired（study-plan.ts:7），但生成即 active（generate-study-plan.ts:86），"草稿"概念形同虚设，与 Phase 9.7"草稿还是激活态是否清晰"的验收点冲突。

---

## 三、问题清单（按严重度）

| 严重度 | 模块 | 问题 | 证据 | 违反条款 |
|--------|------|------|------|----------|
| P1 | learning / question | GenerateQuestion 不回写 SessionItem.questionInstanceId，SubmitAttempt 依赖它，端到端必须外部手工补字段，无任何用例负责绑定 | generate-question.ts:64-73；submit-attempt.ts:83；learning-flow.test.ts:335 | domain-model 关系一致性 / Phase 6.3 |
| P1 | learning | StudyDay.correctCount 用 ReviewRating!=="again" 推导，未读 Evaluation.isCorrect，答错但选 good 会被计为正确 | finalize-review.ts:184；对照 evaluation.ts:11 | BR-022 / 全局硬约束 3 |
| P2 | learning | `canTransitionAttempt`、`canTransitionSession` 定义后零调用，状态机检查散落在 application 层内联，domain 守卫形同虚设 | attempt.ts:24；session.ts:32；调用点 finalize-review.ts:114、end-session.ts:38 | Phase 2.2 / 状态机强制 |
| P2 | learning | SessionItem 允许 pending 直跳 completed，跳过 active，无 canTransitionSessionItem | finalize-review.ts:171-172；session.ts:20 | Phase 8.5 |
| P2 | study-plan | StudyPlan 的 draft/expired/completed 状态永不可达，生成即 active | study-plan.ts:7；generate-study-plan.ts:86 | Phase 9.7 |
| P2 | schema / identity | domain 有 Credential 实体与仓库接口，prisma schema 无 Credential 模型、User 无 passwordHash | user.ts:18；repositories.ts:25；register-user.ts:104；schema.prisma:126-148 | Phase 1.1 / schema-domain 一致 |
| P2 | shared | BR-071 编号重载：StudyDay 汇总 与 注册事务 两个不变量共用同一编号 | study-day.ts:2；register-user.ts:2；golden-scenario.test.ts:177；identity.test.ts:38 | business-rules 编号唯一性 |
| P2 | learning | `createInitialLearningStateDraft` 导出但零调用；注释引用的 `createInitialLearningState` 函数名不存在 | learning-state.ts:26,39；实际走 finalize-review.ts:129 scheduler.initialize | BR-042 / Phase 2.4 |
| P2 | question | QuestionType 多出 `ordering` 且无任何处理分支；Evaluator 不按题型分支，fill_blank/recognition/ordering 评分未实现 | question.ts:8；evaluator.ts:43-72；evaluate-attempt.ts:90 | Phase 6.1 / BR-021 |
| P3 | learning | StudyDay.attemptCount 名实不符，实际统计复习次数（FinalizeReview 内 +1），SubmitAttempt 不累计 | finalize-review.ts:183；study-day.ts:13 | 术语一致性 |
| P3 | question | BR-021"三科目各自 Evaluator"仅注释承诺，代码只有一个通用 CanonicalMatchEvaluator | evaluator.ts:2-3,33 | Phase 6.4 |
| P3 | study-plan | StudyPlanSource 定义 ai/manual，但 GenerateStudyPlan 恒写 scheduler，无 AI/manual 入口 | study-plan.ts:6；generate-study-plan.ts:85 | Phase 9.5 |

---

## 四、question-model.md 缺失影响评估

- **已落地、不受缺失影响的部分**：模板/实例的数据结构（QuestionTemplate.knowledgePointType+type+config+enabled、QuestionInstance.sessionItemId+knowledgePointId+templateId+sequence）在 domain 与 schema 双侧一致（question.ts:10-27；schema.prisma:366-395），`@@unique([knowledgePointType, type])` 约束也在（schema.prisma:375）。
- **因缺失独立规格而悬空的部分**：
  1. 三种题型的**展示契约**（recognition 选项集/干扰项从哪来、fill_blank 挖口串怎么表示、ordering 排序对象）——QuestionInstance 无 payload 字段，config 为无 schema Json，无文档定义。
  2. 三种题型的**评分契约**——Evaluator 接口虽统一（EvaluationResult，evaluation.ts:19），但题型相关的判分规则（部分给分、多选阈值、顺序匹配）无规格，代码里只有归一化字符串匹配。
  3. 题型与知识点类型的**绑定矩阵**——generate-question.ts:52 靠 `findTemplate(kp.type, type)`，但哪些 kp.type 允许哪些题型、缺模板时的降级策略（evaluate-attempt.ts:90 静默退 free_recall）无规格。
- **结论**：缺失 question-model.md 不影响已实现的 free_recall 主链路，但使"题型扩展无需改学习引擎"（Phase 6.6）这一承诺无法验收——当前题型既无独立规格，代码也无题型分支实现。建议在 Phase 6 启动前补 question-model.md，至少钉死题型枚举（去掉或正式纳入 ordering）、payload schema、分题型评分规则三件事。

---

## 五、未覆盖风险

1. **无 Prisma 持久层实现**：infrastructure 仅有 fsrs 与 scrypt，schema 的所有 @relation/UNIQUE 约束是否在真实 PostgreSQL 上生效、事务（FinalizeReview/RegisterUser）是否真实 `$transaction`，当前只能靠内存仓储推断，无法核验（内存事务直接 return fn()，见 golden-scenario.test.ts:30）。
2. **时区边界**：`getLocalDate` 由外部注入（finalize-review.ts:37），本仓库内无按 User.timezone 计算 localDate 的真实实现，BR-061/093 的"业务层零 server timezone"未被代码验证。
3. **并发/竞态未覆盖**：UNIQUE 约束兜底的幂等（BR-012/030）在内存仓库中无并发场景测试，网络重试/并发提交下的真实行为未知。
4. **Content/AI/Governance 三域仅有 domain 类型，无任何用例**：content.ts、conversation.ts、content-issue.ts 均为纯类型定义，其状态机（ContentItem draft→review→published）与 BR-080/082/100/101/102 无实现可审。

---

*本报告仅做只读审核，未修改任何代码或规格文件。*
